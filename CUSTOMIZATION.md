# Website customization

See [README.md](README.md) for the current content, layout, preview, and GitHub Pages publishing instructions.

The maintained content is in `content/site.json`. After editing it, run `python3 scripts/build.py` and `python3 scripts/check.py`, and commit the generated HTML alongside the source change.
