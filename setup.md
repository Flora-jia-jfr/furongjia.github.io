# Setup

This site requires Python 3.9 or newer for generating and checking its static HTML. Browsing and GitHub Pages hosting require no Python runtime.

```sh
python3 scripts/build.py
python3 scripts/check.py
python3 -m http.server 8000
```

Open http://localhost:8000 . See [README.md](README.md) for editing and deployment instructions.
